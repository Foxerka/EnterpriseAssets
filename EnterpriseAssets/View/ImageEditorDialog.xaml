<Window x:Class="EnterpriseAssets.View.ImageEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование фото" 
        Height="550" 
        Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FA">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Кнопки поворота -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button x:Name="BtnRotateLeft" Content="↺ Повернуть влево" Click="RotateLeft_Click" Padding="15,8" Margin="5" Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button x:Name="BtnRotateRight" Content="↻ Повернуть вправо" Click="RotateRight_Click" Padding="15,8" Margin="5" Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button x:Name="BtnReset" Content="⟲ Сбросить" Click="Reset_Click" Padding="15,8" Margin="5" Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <!-- Изображение -->
        <Border Grid.Row="1" Background="#1a1a2e" CornerRadius="8" Padding="10">
            <Grid>
                <Image x:Name="PreviewImage" Stretch="Uniform" />

                <!-- Квадрат обрезки -->
                <Border x:Name="CropBorder"
                        BorderBrush="#4A6FA5"
                        BorderThickness="3"
                        Background="#4A6FA580"
                        Cursor="SizeAll"
                        MouseLeftButtonDown="CropBorder_MouseLeftButtonDown"
                        MouseMove="CropBorder_MouseMove"
                        MouseLeftButtonUp="CropBorder_MouseLeftButtonUp">

                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="0" BlurRadius="15" Opacity="0.8" Color="#4A6FA5"/>
                    </Border.Effect>

                    <Grid>
                        <!-- Желтый уголок для изменения размера -->
                        <Border x:Name="ResizeHandle"
                                Width="15" Height="15"
                                Background="Yellow"
                                BorderBrush="Orange"
                                BorderThickness="2"
                                CornerRadius="2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Margin="0,0,-7,-7"
                                Cursor="SizeNWSE"
                                MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                                MouseMove="ResizeHandle_MouseMove"
                                MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.5"/>
                            </Border.Effect>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Панель управления размером -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="10" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="BtnSizeDown" Content="− Уменьшить область" Click="SizeDown_Click" Padding="20,8" Margin="5" Background="#95A5A6" Foreground="White" Cursor="Hand"/>
                <Button x:Name="BtnCenter" Content="⟲ Центрировать" Click="Center_Click" Padding="20,8" Margin="5" Background="#4A6FA5" Foreground="White" Cursor="Hand"/>
                <Button x:Name="BtnSizeUp" Content="+ Увеличить область" Click="SizeUp_Click" Padding="20,8" Margin="5" Background="#95A5A6" Foreground="White" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Кнопки OK/Cancel -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnCancel" Content="Отмена" Click="Cancel_Click" Padding="25,10" Margin="5" Background="Transparent" BorderBrush="#ccc" BorderThickness="1" Cursor="Hand"/>
            <Button x:Name="BtnSave" Content="Сохранить" Click="Save_Click" Padding="25,10" Margin="5" Background="#4A6FA5" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>