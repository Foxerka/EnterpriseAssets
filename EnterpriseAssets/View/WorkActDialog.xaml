<Window x:Class="EnterpriseAssets.View.Pages.WorkActDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Акт работ" 
        Height="720" 
        Width="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F7FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="White" Padding="20" CornerRadius="8" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock x:Name="TxtTitle" 
                           Text="Создание акта работ" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="#2C3E50"/>
                <TextBlock Text="Заполните информацию о выполненных работах" 
                           FontSize="12" 
                           Foreground="#7F8C8D" 
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Форма -->
        <Border Grid.Row="1" Background="White" Padding="20" CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Первая строка: Номер + Дата -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Номер акта *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtActNumber" 
                                     Height="35" 
                                     Padding="10,0" 
                                     VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Дата работ *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <DatePicker x:Name="DpWorkDate" Height="35"/>
                        </StackPanel>
                    </Grid>

                    <!-- Оборудование -->
                    <TextBlock Text="Оборудование *" FontWeight="SemiBold" Margin="0,15,0,5"/>
                    <ComboBox x:Name="CmbEquipment" 
                              Height="35" 
                              Margin="0,0,0,0"/>

                    <!-- Актив (материал) -->
                    <TextBlock Text="Актив / материал" FontWeight="SemiBold" Margin="0,15,0,5"/>
                    <ComboBox x:Name="CmbAsset" 
                              Height="35"/>

                    <!-- Количество -->
                    <TextBlock Text="Количество" FontWeight="SemiBold" Margin="0,15,0,5"/>
                    <TextBox x:Name="TxtQuantity" 
                             Height="35" 
                             Padding="10,0" 
                             VerticalContentAlignment="Center"/>

                    <!-- Мастер -->
                    <TextBlock Text="Исполнитель *" FontWeight="SemiBold" Margin="0,15,0,5"/>
                    <ComboBox x:Name="CmbMaster" 
                              Height="35"/>

                    <!-- Описание работ -->
                    <TextBlock Text="Описание работ *" FontWeight="SemiBold" Margin="0,15,0,5"/>
                    <TextBox x:Name="TxtDescription" 
                             Height="100" 
                             TextWrapping="Wrap" 
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Padding="10"/>

                    <!-- Статус -->
                    <TextBlock Text="Статус *" FontWeight="SemiBold" Margin="0,15,0,5"/>
                    <ComboBox x:Name="CmbStatus" 
                              Height="35" 
                              SelectedIndex="0"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Кнопки -->
        <Border Grid.Row="2" Background="White" Padding="15" CornerRadius="8" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" 
                        Click="BtnCancel_Click"
                        Background="#95A5A6"
                        Foreground="White"
                        Width="120"
                        Height="40"
                        Margin="0,0,10,0"
                        Cursor="Hand"
                        BorderThickness="0"/>
                <Button Content="💾 Сохранить" 
                        Click="BtnSave_Click"
                        Background="#27AE60"
                        Foreground="White"
                        Width="150"
                        Height="40"
                        Cursor="Hand"
                        BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>